import { PostFormDefinition } from './post-form-definition.model';

describe('PostFormDefinition', () => {
  it('should create an instance', () => {
    expect(new PostFormDefinition()).toBeTruthy();
  });
});
