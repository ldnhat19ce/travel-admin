import { ConvertDropdownPipe } from './convert-dropdown.pipe';

describe('ConvertDropdownPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertDropdownPipe();
    expect(pipe).toBeTruthy();
  });
});
